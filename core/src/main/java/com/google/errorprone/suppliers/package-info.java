/**
 * Supports matchers, but rather than giving Matcher implementations which are predicates on individual AST nodes,
 * a supplier gives contextual information from the traversal of the AST.
 */
package com.google.errorprone.suppliers;
provides:
  - test_helpers

depends:
  - annotation
  - annotations
  - check_api

env:
  SET_VERSION_OVERRIDE: "2.3.4-$GIT_BRANCH-SNAPSHOT"

buildpack:
  host: git.hubteam.com
  organization: HubSpotProtected
  repository: Blazar-Buildpack-Java
  branch: v2
